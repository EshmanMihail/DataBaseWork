@using ModelsLibrary.Models
@{
	var result = ViewBag.data as List<HeatConsumer> ?? new();
	var text = ViewBag.text ?? "";
	var column = ViewBag.column ?? "";
}
<link rel="stylesheet" type="text/css" href="/css/ForSearch.css" />
<a href='@Url.Action("Index", "Home")' class="button">Вернуться на главную</a>
<br>
<br>
<form>
	<label for="searchColumn">Выберите столбец для поиска:</label><br />
	<select name="searchColumn">
		<option value="ConsumerName" @(column == "ConsumerName" ? "selected" : "")>ConsumerName</option>
		<option value="NodeNumber" @(column == "NodeNumber" ? "selected" : "")>NodeNumber</option>
		<option value="CalculatedPower" @(column == "CalculatedPower" ? "selected" : "")>CalculatedPower</option>
	</select><br />

	<label for="searchText">Введите текст для поиска:</label><br />
	<input type="text" name="searchText" value="@text" /><br />

	<input type="submit" value="Найти" />
</form>

<div>
    <table border="1">
        <tr>
            <th>Код</th>
            <th>Название</th>
            <th>Потребляемая мощность</th>
        </tr>

        @foreach (HeatConsumer consumer in result)
        {
            <tr>
                <td>@consumer.ID</td>
                <td>@consumer.ConsumerName</td>
                <td>@consumer.CalculatedPower</td>
            </tr>
        }
    </table>
</div>